[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
python_classes = Test* *Tests
python_functions = test_*
testpaths = inscriptions/tests
markers =
    keycloak: tests d'intégration Keycloak (realm dédié, KEYCLOAK_TEST_INTEGRATION=1)
addopts = -ra
